def hello ():
  def printIntList(list[int] l):
    if nil?(l):
	  exit
	end
	puti(head(l))
	puts(", ")
	printIntList(tail(l))
  end

  list[list[int]] mylist

  mylist := (1 # 2 # nil) # nil
  mylist := (3 # nil) # mylist

  for skip; not nil?(mylist); mylist:=tail(mylist) :
    printIntList(head(mylist))
	puts("\n")
  end
end
